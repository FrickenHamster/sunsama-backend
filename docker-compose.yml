version: "3"
services:
  app:
    build: .
    ports:
      - "3000:8000"
    volumes:
      - .:/opt/app
      - reserved:/opt/app/node_modules/
    entrypoint:
      - npm
      - run
      - develop
volumes:
  reserved:
